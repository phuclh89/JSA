import oracledb from 'oracledb';
import { connectionConfig } from './migration-core.js';
import { loadDatabaseEnvironment } from './oracle-runtime.js';
loadDatabaseEnvironment();
const sequences=['SEQ_JSA_BUSINESS_NUMBER','SEQ_JSA_MASTER','SEQ_JSA_VERSION','SEQ_JSA_VER_PROMPT','SEQ_JSA_VER_PROMPT_COV','SEQ_JSA_VER_TASK','SEQ_JSA_VER_HAZARD','SEQ_JSA_VER_CONTROL','SEQ_JSA_VER_BASIC_STEP','SEQ_JSA_VER_STEP_PERF','SEQ_JSA_VER_STEP_SUP','SEQ_JSA_VER_STEP_TOOL','SEQ_JSA_VER_PROC_REF','SEQ_JSA_VER_ATTACHMENT'] as const;
const required=(name:string)=>{const value=process.env[name]?.trim();if(!value)throw new Error(`Missing required Phase 3 bootstrap configuration: ${name}`);return value;};
async function main(){const siteId=required('LOCAL_SITE_ID'),actor=required('PHASE3_BOOTSTRAP_ACTOR');if(!/^\d{1,19}$/.test(siteId))throw new Error('LOCAL_SITE_ID must be a decimal NUMBER(19) value');const connection=await oracledb.getConnection(connectionConfig());try{const migration=await connection.execute<{C:number}>(`SELECT COUNT(*) C FROM JSA_SCHEMA_VERSION WHERE MIGRATION_ID='005' AND STATUS_CODE='APPLIED'`,{}, {outFormat:oracledb.OUT_FORMAT_OBJECT});if(migration.rows?.[0]?.C!==1)throw new Error('Phase 3 bootstrap requires applied migration 005');const ranges=await connection.execute<{RANGE_START:string;RANGE_END:string;V:number}>(`SELECT TO_CHAR(MIN(RANGE_START)) RANGE_START,TO_CHAR(MAX(RANGE_END)) RANGE_END,COUNT(DISTINCT TO_CHAR(RANGE_START)||':'||TO_CHAR(RANGE_END)) V FROM SYS_SITE_SEQUENCE_RANGE WHERE SITE_ID=:siteId AND IS_ACTIVE='Y'`,{siteId},{outFormat:oracledb.OUT_FORMAT_OBJECT});const range=ranges.rows?.[0];if(!range?.RANGE_START||!range.RANGE_END||range.V!==1)throw new Error('Phase 3 bootstrap requires one consistent approved site range');const binds={siteId,...Object.fromEntries(sequences.map((x,i)=>[`s${i}`,x]))};const existing=await connection.execute<{C:number}>(`SELECT COUNT(*) C FROM SYS_SITE_SEQUENCE_RANGE WHERE SITE_ID=:siteId AND SEQUENCE_CODE IN (${sequences.map((_,i)=>`:s${i}`).join(',')})`,binds,{outFormat:oracledb.OUT_FORMAT_OBJECT});if(existing.rows?.[0]?.C===sequences.length){console.log(JSON.stringify({status:'SKIPPED',siteId,configuredSequenceCount:sequences.length}));return;}if((existing.rows?.[0]?.C??0)>0)throw new Error('Phase 3 bootstrap refused: partial sequence-range configuration exists');for(const sequence of sequences)await connection.execute(`ALTER SEQUENCE ${sequence} RESTART START WITH ${range.RANGE_START}`);for(const sequence of sequences){const id=await connection.execute<{ID:string}>(`SELECT TO_CHAR(SEQ_SYS_SITE_SEQ_RANGE.NEXTVAL) ID FROM DUAL`,{}, {outFormat:oracledb.OUT_FORMAT_OBJECT});await connection.execute(`INSERT INTO SYS_SITE_SEQUENCE_RANGE(RANGE_ID,SITE_ID,SEQUENCE_CODE,RANGE_START,RANGE_END,CREATED_BY,UPDATED_BY) VALUES(:id,:siteId,:sequence,:startValue,:endValue,:actor,:actor)`,{id:id.rows?.[0]?.ID,siteId,sequence,startValue:range.RANGE_START,endValue:range.RANGE_END,actor});}await connection.commit();console.log(JSON.stringify({status:'PASS',siteId,configuredSequenceCount:sequences.length}));}catch(error){await connection.rollback();throw error;}finally{await connection.close();}}
main().catch(error=>{console.error(JSON.stringify({status:'FAIL',message:error instanceof Error?error.message:String(error)}));process.exitCode=1;});
